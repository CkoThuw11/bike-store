[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
markers =
    unit: fast tests for domain/application logic without external services
    integration: repository/infrastructure tests using PostgreSQL
    e2e: HTTP contract tests through FastAPI routes
addopts = -ra
